# Security Notes

Do not commit licensed VASP `POTCAR` files, credentials, private account names, or cluster-specific secrets.

Before publishing this repository, review:

- `Templates/sub` for usernames, emails, accounts, and paths
- generated export folders
- any copied VASP potential files
